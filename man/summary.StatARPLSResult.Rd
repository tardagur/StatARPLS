% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{summary.StatARPLSResult}
\alias{summary.StatARPLSResult}
\title{Summarise a StatARPLSResult object}
\usage{
\method{summary}{StatARPLSResult}(object, ...)
}
\arguments{
\item{object}{An object of class \code{StatARPLSResult}.}

\item{...}{Additional arguments passed to or from other methods; unused
here and retained only for S3 signature compatibility with
\code{summary.default()}.}
}
\value{
An object of class \code{summary.StatARPLSResult}, a list containing:
\itemize{
\item \code{lambda} — the selected regularisation parameter
\item \code{n_iter}, \code{converged} — fitting diagnostics
\item \code{residual_variance} — as stored on the original object
\item \code{residual_quantiles} — named numeric vector, the 0/25/50/75/100th
percentiles of \code{corrected}
\item \code{mean_band_width} — numeric scalar, the mean pointwise width of
\code{stability_bounds}
\item \code{gcv} — the object's \code{"gcv"} attribute, or \code{NULL} if absent
\item \code{bootstrap} — the object's \code{"bootstrap"} attribute, or \code{NULL} if
absent
}
}
\description{
Computes residual and stability-band diagnostics for a fitted
\code{StatARPLSResult} object and returns them as a \code{summary.StatARPLSResult}
object with its own \code{print()} method. When available, GCV grid search
and bootstrap replicate metadata are carried through into the summary.
}
