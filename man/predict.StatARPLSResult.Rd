% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.StatARPLSResult}
\alias{predict.StatARPLSResult}
\title{Predict baseline values on a new wavenumber grid}
\usage{
\method{predict}{StatARPLSResult}(object, newdata, method = c("linear", "spline"), rule = 1L, ...)
}
\arguments{
\item{object}{An object of class \code{StatARPLSResult}.}

\item{newdata}{A numeric vector of new wavenumber coordinates at which
to predict the fitted baseline.}

\item{method}{A single character string, either \code{"linear"} (the
default, dispatches to \code{stats::approx()}) or \code{"spline"} (dispatches
to \code{stats::spline()} for smooth cubic interpolation).}

\item{rule}{A single integer, \code{1} or \code{2}, controlling behaviour for
\code{newdata} points outside the fitted wavenumber range. \code{1} (the
default) returns \code{NA} for out-of-range points; \code{2} extends the
boundary value as a constant. Passed through to \code{stats::approx()};
ignored (with a warning) for \code{method = "spline"}, which always
extrapolates.}

\item{...}{Additional arguments passed to or from other methods; unused
here and retained only for S3 signature compatibility with
\code{predict.default()}.}
}
\value{
A numeric vector the same length as \code{newdata}, giving the
predicted baseline value at each requested wavenumber:
\itemize{
\item in-range points — interpolated from the fitted \code{wavenumber} /
\code{baseline} pair
\item out-of-range points — \code{NA} under \code{rule = 1}, or the nearest fitted
boundary value under \code{rule = 2}
}
}
\description{
Interpolates (and, if explicitly requested, extrapolates) a fitted
ARPLS baseline onto an arbitrary target wavenumber grid, using
\code{stats::approx()} for linear interpolation or \code{stats::spline()} for
smooth cubic interpolation. By default, target points falling outside
the range of the originally fitted \code{wavenumber} vector return \code{NA}
rather than a guessed value, since the fitted ARPLS baseline carries no
statistical information beyond the observed spectrum.
}
