% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{compare.StatARPLSResult}
\alias{compare.StatARPLSResult}
\title{Compare multiple ARPLS baseline fits side by side}
\usage{
\method{compare}{StatARPLSResult}(x, ..., labels = NULL)
}
\arguments{
\item{x}{An object of class \code{StatARPLSResult}, used as the first row
of the comparison table.}

\item{...}{One or more additional objects of class \code{StatARPLSResult}
to compare against \code{x}. Objects of any other class trigger an error.}

\item{labels}{An optional character vector of row labels, the same
length as the total number of objects being compared (\code{x} plus all
\code{...} arguments). Defaults to the deparsed argument expressions
(e.g. \code{"fit_low_lambda"}) when not supplied.}
}
\value{
An object of class \code{compare.StatARPLSResult}, a data frame
with one row per compared fit and columns:
\itemize{
\item \code{label} — character, the fit's identifying label
\item \code{lambda} — numeric, the fitted regularisation parameter
\item \code{df_effective} — numeric, effective degrees of freedom from GCV
metadata, or \code{NA} if the fit was not produced via
\code{gcv_select_lambda()}
\item \code{n_iter} — integer, reweighting iterations to convergence
\item \code{converged} — logical, convergence status
\item \code{mean_band_width} — numeric, mean pointwise width of
\code{stability_bounds}
}
}
\description{
Assembles a comparative diagnostics table across two or more
\code{StatARPLSResult} objects, contrasting their selected regularisation
parameter, effective degrees of freedom (where GCV metadata is
present), iterations to convergence, and mean stability-band width.
Useful for contrasting fits at different lambda values, on different
spectra, or before/after applying \code{bootstrap_baseline_uncertainty()}.
}
